# Constructed regression fixtures

The cases in `tests/test_regression_fixtures.py` cover clean inputs, key skew, missing pairs, duplicate records, invalid parses, changed hashes, and annotations that do not mutate scores.

These fixtures were written alongside the implementation. They are regression coverage, not held-out evaluation or evidence of generalization.
