# Saved responses on ARC science items

The frozen `bundle.json` is the replay input. Model attribution is recorded locally; full provider responses and serving revisions are unavailable. [Catalog](../../data/README.md) and [source verification](../../data/provenance/README.md) distinguish item provenance from model execution.

Collection source is retained separately under `scripts/collection/`; no model calls are needed for `make evidence`.
